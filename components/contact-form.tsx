'use client';
import { useState } from 'react'; import { ArrowRight, CheckCircle2 } from 'lucide-react';
export function ContactForm(){const [status,setStatus]=useState<'idle'|'loading'|'success'|'error'>('idle'); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus('loading');const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();setStatus('success');form.reset()}catch{setStatus('error')}}return <form onSubmit={submit} className="contactform"><input className="honey" name="website" tabIndex={-1} autoComplete="off"/><div className="fieldrow"><label>Votre nom *<input required name="name" placeholder="Nom et prénom" maxLength={100}/></label><label>Votre entreprise<input name="company" placeholder="Nom de l’entreprise" maxLength={100}/></label></div><div className="fieldrow"><label>Votre email *<input required name="email" type="email" placeholder="vous@entreprise.com" maxLength={160}/></label><label>Votre téléphone<input name="phone" type="tel" placeholder="+212 …" maxLength={30}/></label></div><div className="fieldrow"><label>Type de projet *<select required name="projectType" defaultValue=""><option value="" disabled>Sélectionnez</option><option>Site web</option><option>Application / SaaS</option><option>IA / Automatisation</option><option>Marketing / SEO</option><option>Conseil digital</option><option>Autre</option></select></label><label>Budget indicatif<select name="budget" defaultValue=""><option value="">À définir ensemble</option><option>Moins de 20 000 MAD</option><option>20 000 – 50 000 MAD</option><option>50 000 – 100 000 MAD</option><option>Plus de 100 000 MAD</option></select></label></div><label>Parlez-nous de votre projet *<textarea required name="message" placeholder="Contexte, objectifs, délais…" minLength={20} maxLength={3000}/></label><label className="consent"><input type="checkbox" required/> J’accepte que YMKEN utilise ces informations pour répondre à ma demande.</label><button className="button" disabled={status==='loading'}>{status==='loading'?'Envoi en cours…':'Envoyer ma demande'} <ArrowRight/></button>{status==='success'&&<p className="formsuccess"><CheckCircle2/> Merci. Votre message a bien été envoyé.</p>}{status==='error'&&<p className="formerror">L’envoi a échoué. Écrivez-nous à contact@ymken.com.</p>}</form>}
