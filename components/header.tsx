'use client';
import Link from 'next/link'; import { Menu, X } from 'lucide-react'; import { usePathname } from 'next/navigation'; import { useState } from 'react'; import { nav } from '@/lib/data'; import { Logo } from './logo';
export function Header() { const [open,setOpen]=useState(false); const path=usePathname(); return <header className="header"><div className="shell nav"><Link href="/" aria-label="Accueil YMKEN"><Logo /></Link><nav className={open?'navlinks open':'navlinks'} aria-label="Navigation principale">{nav.map(n=><Link key={n.href} className={path===n.href?'active':''} onClick={()=>setOpen(false)} href={n.href}>{n.label}</Link>)}<Link className="button navmobile" href="/contact">Parlons de votre projet <span>↗</span></Link></nav><Link className="button navcta" href="/contact">Parlons de votre projet <span>↗</span></Link><button className="menubtn" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label={open?'Fermer le menu':'Ouvrir le menu'}>{open?<X/>:<Menu/>}</button></div></header> }
