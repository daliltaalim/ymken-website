import Link from 'next/link'; import { ArrowRight, MessageCircle } from 'lucide-react'; import { projects } from '@/lib/data';
export function Eyebrow({children,dark=false}:{children:React.ReactNode;dark?:boolean}){return <p className={dark?'eyebrow dark':'eyebrow'}><i/> {children}</p>}
export function PageHero({label,title,text}:{label:string;title:React.ReactNode;text:string}){return <section className="pagehero"><div className="orb one"/><div className="shell"><Eyebrow>{label}</Eyebrow><h1>{title}</h1><p>{text}</p></div></section>}
export function CTA(){const number=process.env.NEXT_PUBLIC_WHATSAPP_NUMBER||'212600000000';return <section className="ctawrap"><div className="shell cta"><div className="ctaart"/><Eyebrow>Votre prochaine étape</Eyebrow><h2>Vous avez un projet digital ?<br/><em>Construisons-le ensemble.</em></h2><p>Une idée, un défi ou un objectif de croissance ? Échangeons sur la solution la plus juste.</p><div className="actions"><Link className="button white" href="/contact">Demander un devis <ArrowRight/></Link><a className="button ghost" href={`https://wa.me/${number}`} target="_blank"><MessageCircle/> WhatsApp</a></div></div></section>}
export function ProjectGrid(){return <div className="projectgrid">{projects.map((p,i)=><article className={`project ${p.tone}`} key={p.name}><div className="projectvisual"><div className="mock"><span>{p.name.slice(0,1)}</span><div/><div/><div/></div><small>0{i+1}</small></div><div className="projectcopy"><p>{p.type}</p><h3>{p.name}</h3><div className="tags">{p.tags.map(t=><span key={t}>{t}</span>)}</div><p>{p.text}</p><Link href={`/realisations#${p.slug}`}>Voir le projet <ArrowRight/></Link></div></article>)}</div>}
export function WhatsApp(){const n=process.env.NEXT_PUBLIC_WHATSAPP_NUMBER||'212600000000';return <a href={`https://wa.me/${n}`} target="_blank" className="whatsapp" aria-label="Nous contacter sur WhatsApp"><MessageCircle/></a>}
